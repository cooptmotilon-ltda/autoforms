const baseFormID = ""; // the form id
const dataBookId = ""; // the data book id
const titlesSheetName = ""; // the data input book id

function main() {
  makeForms(baseFormID, dataBookId, titlesSheetName);
}
